#include<iostream>
#include<vector>
using namespace std;
void merge_sort(vector<int> &v){
    int mid=v.size()/2;
    int a=mid;
    int b=v.size()-1;
}
int main(){
    // taking input
    int n;
    cout<<"Enter the length of the array: ";
    cin>>n;
    cout<<"Enter "<<n<<" elements of the array: \n";
    vector<int> v(n);
    for(int i=0;i<n;i++){
        cin>>v[i];
    }
    // solution
    for(int i=0;i<n;i++){}
    return 0;
}